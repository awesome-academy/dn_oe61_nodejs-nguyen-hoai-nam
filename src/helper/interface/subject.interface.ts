export interface SubjectRow {
    code?: string;
    name: string;
    description: string;
    studyDuration: number;
}

export interface taskRow {
    subCode?: string
    name: string;
    fileUrl: string;
}
